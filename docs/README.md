# Documentation

This folder keeps both current product documentation and older assignment-era material.

## Current references

- `architecture.md` explains the backend/frontend shape and current limitations.
- `class-diagram.md` documents the main backend classes.
- `improvement-report.md` records the backend cleanup already completed before the React frontend work.

## Historical references

The sprint plans, audit notes, implementation write-up, demo plan, screenshots, and original `.docx` files are preserved for project history. They may still mention assignment requirements or console demos, so prefer `README.md` at the repository root for the current product direction.
